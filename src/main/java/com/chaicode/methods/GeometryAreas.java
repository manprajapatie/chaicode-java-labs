package com.chaicode.methods;

/**
 * BuildRight Geometry Toolkit
 *
 * <p>Area calculators for common shapes used in a construction estimator app.
 *
 * <p>Rules:
 * <ul>
 *   <li>circleArea(radius) = π × r², return -1 if radius &lt; 0</li>
 *   <li>rectangleArea(width, height) = w × h, return -1 if either dimension &lt; 0</li>
 *   <li>triangleArea(base, height) = 0.5 × base × height, return -1 if either &lt; 0</li>
 *   <li>Use Math.PI for π</li>
 *   <li>Round each result to 2 decimals: Math.round(value * 100) / 100.0</li>
 * </ul>
 */
public final class GeometryAreas {

    private GeometryAreas() {}

    public static double circleArea(double radius) {
        // Your code here
        return 0.0;
    }

    public static double rectangleArea(double width, double height) {
        // Your code here
        return 0.0;
    }

    public static double triangleArea(double base, double height) {
        // Your code here
        return 0.0;
    }
}
