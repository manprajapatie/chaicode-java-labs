package com.chaicode.forloop;

/**
 * MathLab Factorial Machine
 *
 * <p>Compute n! (n factorial) using a for loop.
 *
 * <p>Rules:
 * <ul>
 *   <li>0! = 1</li>
 *   <li>If n is negative, return -1</li>
 *   <li>If result overflows int range, return -1 (stop if product would exceed Integer.MAX_VALUE)</li>
 * </ul>
 *
 * @param n non-negative integer
 * @return factorial, or -1 for invalid input or overflow
 */
public final class FactorialCalculator {

    private FactorialCalculator() {}

    public static int factorial(int n) {
        // Your code here
        return 0;
    }
}
