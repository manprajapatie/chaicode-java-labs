package com.chaicode.forloop;

/**
 * CryptoCheck Prime Validator
 *
 * <p>A security tool checks whether a number is prime using a for loop.
 *
 * <p>Rules:
 * <ul>
 *   <li>Numbers less than 2 are not prime</li>
 *   <li>2 is prime</li>
 *   <li>Check divisibility from 2 to sqrt(n) inclusive</li>
 * </ul>
 *
 * @param n the number to check
 * @return true if prime, false otherwise
 */
public final class PrimeChecker {

    private PrimeChecker() {}

    public static boolean isPrime(int n) {
        // Your code here
        return false;
    }
}
