package com.chaicode.dowhileloop;

/**
 * SliceHouse Pizza Orders
 *
 * <p>Count how many pizzas were ordered from a list of order codes until "DONE" appears.
 * Uses a do-while loop to process at least one entry when the array is non-empty.
 *
 * <p>Rules:
 * <ul>
 *   <li>Count non-null, non-empty, non-"DONE" entries until "DONE" is seen</li>
 *   <li>"DONE" itself is not counted</li>
 *   <li>If orders is null or empty, return 0</li>
 * </ul>
 *
 * @param orders array of order codes, ending with "DONE"
 * @return number of pizzas ordered
 */
public final class PizzaOrders {

    private PizzaOrders() {}

    public static int countOrders(String[] orders) {
        // Your code here
        return 0;
    }
}
