package com.chaicode.strings;

/**
 * WordPlay Palindrome Checker
 *
 * <p>Check if a phrase is a palindrome, ignoring case, spaces, and punctuation.
 *
 * <p>Examples: "Race car" → true, "A man, a plan, a canal: Panama" → true, "hello" → false
 *
 * <p>Rules:
 * <ul>
 *   <li>Keep only letters and digits, compare forward vs backward (case-insensitive)</li>
 *   <li>If text is null, return false</li>
 *   <li>Empty after cleaning is considered a palindrome</li>
 * </ul>
 *
 * @param text input phrase
 * @return true if palindrome
 */
public final class PalindromeChecker {

    private PalindromeChecker() {}

    public static boolean isPalindrome(String text) {
        // Your code here
        return false;
    }
}
